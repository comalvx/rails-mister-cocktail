<h1><%= @cocktail.name %></h1>
<% @cocktail.doses.each do |dose| %>
  <p>Dose : <%= dose.description %></p>
  <p>Ingrédient : <%= dose.ingredient.name %><%= link_to "Delete", dose_path(dose),
      method: :delete, data: { confirm: "Are you sure?" }
    %></p>
<% end %>

